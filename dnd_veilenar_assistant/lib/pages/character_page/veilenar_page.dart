import 'package:flutter/material.dart';

class VeilenarPage extends StatelessWidget {
  const VeilenarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Персонаж Veilenar'),
      ),
    );
  }
}
