import 'package:flutter/material.dart';

class PhBPage extends StatelessWidget {
  const PhBPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Персонаж PhB'),
      ),
    );
  }
}
