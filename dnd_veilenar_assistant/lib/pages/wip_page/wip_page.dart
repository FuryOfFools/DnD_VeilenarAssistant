import 'package:flutter/material.dart';

class WIPPage extends StatelessWidget {
  const WIPPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('Виджет не завершен'),
        ),
      ),
    );
  }
}
